
请分析以下输入，构建知识图谱并给出教学讲解：

---

{{text}}

---

提醒：
- 先判断这是**提问**还是**陈述**，提问型请在 summary 开头直接给出答案
- 图谱中必须包含一个**领域锚点**（type: `domain`）和一个**问题背景**（type: `problem`），它们是图谱的骨架
- 实体要跨越多个层次（领域 → 问题 → 核心 → 机制 → 应用），不要集中在同一层次
