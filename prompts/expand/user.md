用户正在探索实体「{{entityName}}」（id: `{{entityId}}`）。

---

## 原始上下文

用户最初输入的文本/问题如下，这是整个知识图谱的**锚点**，所有新实体都应与此保持关联：

```
{{originalText}}
```

---

## 当前图谱中的已有实体

建立关系时**必须使用以下已有实体的 id**，不要自行编造 id：

{{existingEntities}}

---

## 你的任务

1. 对「{{entityName}}」进行深度教学讲解（参考系统提示的结构）
2. 提取 3~6 个新实体，每个新实体必须能回答"为什么理解「{{entityName}}」需要知道它"
3. 建立新实体与已有实体（或新实体之间）的关系，优先使用已有实体 id 建立连接
4. 给出 3~5 个建议探索方向，帮用户决定下一步往哪里走
